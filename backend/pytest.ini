[pytest]
# Marcadores declarados: sin esto pytest los avisa como posibles erratas, y un
# aviso que se ve siempre deja de verse.
markers =
    postgres: exige un PostgreSQL real (SEIS_TEST_POSTGRES_URL). Se omite sin él.
